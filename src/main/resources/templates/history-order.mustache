<!DOCTYPE HTML>
<head>
    <title>ВКР МУИВ Триппель Анастасия</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <style>
        table {
            width: 85%;
            margin: 30px auto;
            border-collapse: collapse;
        }
        thead {
            background-color: #2c87c8;
            color: #ffffff;
        }
        th,
        td {
            padding: 10px;
            border: 1px solid #667666;
        }
        form {
            display: inline-block;
        }
    </style>
</head>

<body>
<div>
    <div style="float: left">
        <form id="home" action="/" target="_self">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <button>Вернуться на<br>Стартовую страницу</button>
        </form>
    </div>
    <div>
        <form id="home" action="/orders" target="_self">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <button>Перейти на страницу<br>История Заказа</button>
        </form>
    </div>
    <div style="float: right">
        <form action="/logout" method="post">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <input type="submit" value="Sign Out"/>
        </form>
    </div>
</div>

<h1 style="text-align: center; margin-top: 20px; margin-bottom: 20px;">История заказа № {{id}}</h1>
    <table>
        <thead>
        <tr>
            <th>Количество</th>
            <th>Название</th>
            <th>Цена</th>
            <th>Изображение</th>
            <th>Название поставщика</th>
        </tr>
        </thead>
        {{#items}}
            <tr>
                <td>
                    {{quantity}}
                </td>
                <td>{{title}}</td>
                <td>{{cost}} &#8381;</td>
                <td><img src="{{urlPhoto}}" alt="{{title}}" width="250" height="200"></td>
                <td>{{organization.title}}</td>
            </tr>
        {{/items}}
    </table>
</body>

